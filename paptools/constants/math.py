from ..core.number import Number
import numpy as np
import sympy as sp

pi = Number(np.pi, None, symbol="pi", unit_str=None)
e = Number(np.e, None, symbol="e", unit_str=None)
tau = Number(2 * np.pi, None, symbol="tau", unit_str=None)

